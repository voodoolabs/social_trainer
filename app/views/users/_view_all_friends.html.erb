<% if current_user %>
  <% @users.each do |user| %>
  <h1><%= link_to image_tag(user.photo_url, {height:45}) + user.name, url_for(user_path(user.id)) %></h1>
  <%= link_to "Add as friend", user_path(user.id) %>
    <% end %>
  <% end %>

