require 'spec_helper'

describe UsersController do
  describe "#show" do
    it "gets a specific user from the database" do
      user = double(:user, :id => 2, )
      # => pass in a user, id, name
      # => pass in 2+ mock posts
      # => test for posts on page

    end
    it "gets a specific user from the database" do

    end
    it "gets a specific user from the database" do

    end
    it "gets a specific user from the database" do

    end
  end

  it "welcome page requires http authentication"

  it "welcome page username is 'geek' and password is 'jock'"

  it "non-welcome pages do not require http auth"
end
